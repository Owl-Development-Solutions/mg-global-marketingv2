import { GenealogyFlattenedPipe } from './genealogy-flattened.pipe';

describe('GenealogyFlattenedPipe', () => {
  it('create an instance', () => {
    const pipe = new GenealogyFlattenedPipe();
    expect(pipe).toBeTruthy();
  });
});
